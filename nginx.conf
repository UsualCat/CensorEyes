# Nginx Configuration Snippet for CensorEyes
# Place this inside your server {} block

# Block direct access to SQLite database files (.db, .wal, .shm) and markdown files
location ~ \.(db|sqlite|sqlite3|wal|shm|md)$ {
    deny all;
    return 404;
}
